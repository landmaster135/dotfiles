# 表示位置（top-left, top-right, bottom-left, bottom-right）
position=top-left

# FPS
fps
frametime

# GPU
gpu_stats
gpu_temp
gpu_mem_used
gpu_mem_total

# CPU
cpu_stats
cpu_temp

# メモリ
ram
vram

# バッテリー（ノートPC）
battery
battery_watt
battery_time

# 見た目
background_alpha=0.5
font_size=20

# Autostart overriders

## Fcitx5
`./.config/autostart/org.fcitx.Fcitx5.desktop` が該当する。

`~/.config/autostart/org.fcitx.Fcitx5.desktop` の設定ファイルに `Hidden=true` を追加すると、システム側のファイルを書き換えずに Fcitx5 の自動起動だけを無効化できます。
